API Specification:

1. Endpoint: `/api/v1/chose_next_speaker`
   Method: `GET`
   Parameters:
      - `candidates` (list[str]): List of candidates
      - `messages` (list[str]): List of messages
   Returns: str | None: Selected next speaker or None if nobody is chosen

2. Endpoint: `/api/v1/get_response`
   Method: `GET`
   Parameters:
      - `ego` (str): The ego representing the speaker
      - `messages` (list[str]): List of messages
   Returns: str: The response generated by the ego

3. Endpoint: `/api/v1/converse`
   Method: `GET`
   Parameters:
      - `egos` (list[str]): List of egos
      - `messages` (list[str], optional, default=[]): List of messages
      - `limit` (int, optional): Limit of conversation rounds
   Returns: list[str]: The final conversation messages

4. Endpoint: `/api/v1/extract_ego_from_description`
   Method: `GET`
   Parameters:
      - `description` (str): Description to extract ego from
   Returns: str: The extracted ego from the description

5. Endpoint: `/api/v1/grow_egos`
   Method: `GET`
   Parameters:
      - `content` (str): Content to grow egos from
      - `egos` (list[str], optional, default=[]): List of existing egos
   Returns: list[str]: The grown egos

6. Endpoint: `/api/v1/extract_insights_from_conversations`
   Method: `GET`
   Parameters:
      - `egos` (list[str]): List of egos
      - `messages` (list[str], optional, default=[]): List of messages
      - `name` (str, optional, default="someone"): Name of the person
      - `pronoun` (str, optional, default="them"): Pronoun to use
      - `reflexive_pronoun` (str, optional, default="themself"): Reflexive pronoun to use
   Returns: str: Extracted insights from the conversations